/**
 * @usage - This file is used to define common proptypes
 * which will be used all over the app frequently
 */

// a function that does nothing, This replaces the ramda's noop function,
// as that has additional req which we don't need
export const noop = () => {};
