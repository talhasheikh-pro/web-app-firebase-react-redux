import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
    root: {
        minWidth: 275,
        width: 400,
        textAlign: 'center',
        margin: '0 auto',
        marginTop: 300,
        padding: 20,
    },
    btn: {
        margin: '0 auto',
    },
    title: {
        fontSize: 14,
    },
});

export default useStyles;
